# jishu-sdk-kotlin
Kotlin package for Jishu Promo access. Adds a simple import Jishu integration for Android apps to expose a shareable app-scoped user   ID, check promo access through the Jishu API, and combine Jishu access with RevenueCat entitlements.
